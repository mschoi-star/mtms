# Docker Compose 실행 시 DATABASE_URL은 docker-compose.yml에서 자동 주입됩니다.
# 로컬 직접 실행(uvicorn) 시에만 아래 값을 설정하세요.
DATABASE_URL=postgresql://postgres:password@localhost:5432/lgedb
SECRET_KEY=change-me-in-production
DEBUG=false
